# Example environment variables for deployment
# Copy to .env.local for local dev, or set in Vercel dashboard for production

# Production: Set to your deployed backend URL (no trailing slash)
VITE_API_BASE_URL=https://your_backend_url.com

# Local dev: Use proxy (comment out the above, use this)
# VITE_API_BASE_URL=/api
