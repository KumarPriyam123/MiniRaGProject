# Backend dependencies
fastapi
uvicorn[standard]
pydantic
pydantic-settings
python-dotenv
python-multipart

# ML & Embeddings  
# NOTE: Using Cohere API for embeddings instead of local model
# This avoids OOM on Render free tier (512MB limit)
tiktoken

# Vector DB
pinecone

# Reranker + Embeddings (API-based, no local model)
cohere

# LLM
groq

# Text processing
langchain-text-splitters

# File extraction
PyMuPDF
python-docx
